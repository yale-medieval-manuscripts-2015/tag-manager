<h3>All tags</h3>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Category</th>
      <th>Tag</th>
      <th>Label</th>
        <th>Description</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tags.each do |tag| %>
      <tr>
        <td><%= tag.category %></td>
        <td><%= tag.tag %></td>
        <td><%= tag.label %></td>
        <td><%= tag.description %></td>
        <td><%= link_to 'View', tag %></td>
        <td><%= link_to 'Edit', edit_tag_path(tag) %></td>
        <td><%= link_to 'Delete', tag, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tag', new_tag_path %>
<br><br><br>
